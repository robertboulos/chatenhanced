# Task ID: 6
# Title: Implement Session Management Endpoints
# Status: pending
# Dependencies: 5
# Priority: high
# Description: Create working CRUD endpoints for chat session management
# Details:
Implement POST /sessions (create), GET /sessions (list), GET /sessions/:id (retrieve), PUT /sessions/:id (update), DELETE /sessions/:id (delete). Link sessions to companions and users properly.

# Test Strategy:
Test full CRUD cycle for sessions with proper data persistence
